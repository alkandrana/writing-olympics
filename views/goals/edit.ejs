<% title = `Edit ${goal.title}` %>
<h1>Edit Goal</h1>
<form method="post" action="/goals/edit/<%= goal.id %>">
    <div>
        <label for="title">Title</label>
        <input type="text" name="title" id="title" value="<%= locals.goal.title %>"/><br>
    </div>
    <div>
        <label for="startDate">Start Date</label>
        <input type="text" name="start" id="startDate" value="<%= locals.goal.start %>"><br>
    </div>
    <div>
        <label for="endDate">End Date</label>
        <input type="text" name="stop" id="endDate" value="<%= locals.goal.stop %>">
    </div>

  <div>
      <label for="target">Goal Target</label>
      <input type="number" name="target" id="target" value="<%= locals.goal.target %>"><br>
  </div>

  <div>
      <button type="submit" class="btn btn-success">Save Changes</button>
      <a href="/goals/<%= goal.id %>" class="btn btn-warning">Cancel</a>
  </div>
</form>
